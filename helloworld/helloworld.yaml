- name: remediate_stress_process
  hosts: "{{ip}}"
  tasks:

   - name: killstress
     shell: |
           ps -ef | grep "stress-ng" 
           for pid in $(ps -ef | grep "stress-ng" | awk '{print $2}'); do kill -9 $pid; done 
     register: stdd
     ignore_errors: false

   - debug: msg="{{ stdd.stdout }}"
   - debug: msg="{{ stdd.stderr }}"
