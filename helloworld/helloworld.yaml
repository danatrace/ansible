- name: remediate_stress_process
  hosts: "{{ip}}"
  connection: ssh
  tasks:

   - name: killstress
     win_shell: |
           cmd: '"C:\Users\Administrator\Downloads\cleaner.bat" "argument 1" -stop'
     register: stdd
     ignore_errors: true


   - debug: msg="{{ stdd.stdout }}"
   - debug: msg="{{ stdd.stderr }}"
